#include "Emulator.hpp"

int main(int argc, char* argv[]) {

    if (argc < 2) {
        std::cerr << "Error, invalid arguments." << std::endl;
        return -1;
    }

    // Elf32File program(argv[1], );

    return 0;
}