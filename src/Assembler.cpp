#include "../inc/Assembler.hpp"
#include "../build/bison.tab.h"

int Assembler::location_counter = 0;

int Assembler::startAssembler() {
  
    return 0;
}

void Assembler::increaseLocationCounter() { location_counter++; }